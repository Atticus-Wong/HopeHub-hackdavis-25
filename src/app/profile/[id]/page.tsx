import { DataTable as DataTableType } from "@/types/types"

type ProfilePageProps = {
  params: {
    id: string
  }
  profileData: DataTableType
}
export default function ProfilePage({params, profileData}: ProfilePageProps) {

  return (
    <div>
    </div>
  )
}